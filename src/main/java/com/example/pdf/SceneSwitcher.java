package com.example.pdf;

import javafx.fxml.FXMLLoader;
import javafx.scene.Node;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;

import java.io.IOException;

/**
 * Utility class for switching between different scenes in the application.
 */
public class SceneSwitcher {

    /**
     * Switches the current scene to a new one specified by the FXML file.
     *
     * @param fxmlFile The FXML file name located in the resources folder.
     * @param node     The current node (usually obtained from an event source).
     */
    public static void switchScene(String fxmlFile, Node node) {

    }

    public static void switchTo(String s) {
    }
}
